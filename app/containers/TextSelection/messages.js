/*
 * TextSelection Messages
 *
 * This contains all the text for the TextSelection component.
 */
import { defineMessages } from 'react-intl';
import messages from './messages.json';

export default defineMessages(messages);
